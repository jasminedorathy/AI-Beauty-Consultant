fastapi>=0.110.0,<0.120.0
uvicorn[standard]>=0.29.0,<0.32.0
pydantic[email]>=2.6.0,<2.10.0
python-multipart>=0.0.9
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
argon2-cffi>=21.3.0
pyotp>=2.9.0
qrcode[pil]>=7.4.0
pymongo>=4.6.0,<5.0.0
python-dotenv>=1.0.0
requests>=2.31.0
Pillow>=10.0.0,<11.0.0
numpy>=1.24.0,<2.0.0
opencv-python-headless>=4.8.0,<4.11.0
mediapipe>=0.10.9,<0.11.0
pytesseract>=0.3.10
dnspython>=2.0.0
scikit-learn>=1.4.0
reportlab>=4.1.0
psutil>=5.9.0
httpx>=0.27.0
razorpay>=1.4.1

# ── Redis (optional — for multi-worker rate limiting) ─────────────────────────
redis>=5.0.0  # optional: install for production multi-worker rate limiting

# ── Cloudinary (optional — private object storage for biometric images) ───────
# Set CLOUDINARY_CLOUD_NAME / CLOUDINARY_API_KEY / CLOUDINARY_API_SECRET to activate.
# Falls back to base64-in-MongoDB when not configured (dev/test only).
cloudinary>=1.38.0,<2.0.0

# ── Deep learning model backends (skin + face-shape analysis) ────────────────
# Both are imported LAZILY and gated by a memory check (see
# app/core/model_memory_guard.py) — on a low-memory host (e.g. Render free
# tier, ~512MB RAM) neither will actually be imported/loaded, and the app
# automatically falls back to the existing OpenCV/geometry heuristics. This
# means it's safe to ship these dependencies even on constrained hosting;
# they simply activate once you deploy to a host with >= ~1-1.5GB RAM.
#
# For production, prefer CPU-only wheels to reduce install size/memory:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
torch>=2.2.0,<2.4.0            # used by app/ml/face_shape_predictor.py (EfficientNetV2S)
torchvision>=0.17.0,<0.19.0
tensorflow-cpu>=2.15.0,<2.17.0 # used by app/ml/skin_model_loader.py (DenseNet-201 skin model)
